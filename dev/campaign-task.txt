Okay, uh, the weekly numbers keep arriving in three different spreadsheets. Can we make one small page for the sales team? It should show revenue, returns, and yesterday's orders. Actually, start with a CSV upload. No direct database access for this first version. Um, keep the amounts in Czech crowns, and flag rows where the order identifier is missing. I want a preview before anything is saved. Success means the totals match the uploaded file, and bad rows have a useful error. I haven't picked an owner or a deadline yet.
