# What Mluva can show

Source audit: the September 2026 delight launch implementation, including the integrated logo, stability and earlier film work from PRs #17–19. This describes implementation behavior; the [feature-maturity register](feature-maturity.md) remains the authority for manual acceptance. The [launch direction](delight-launch-direction.md) defines this pass, and individual media manifests distinguish real-provider captures from fixture demonstrations.

## Capability matrix

| Capability | Actual behavior and a useful demonstration | Boundary | Source and evidence |
| --- | --- | --- | --- |
| Dictation | F9 starts/stops capture; completed text copies by default. Show speech becoming an editable original. Raw recognition remains recoverable. | Recording/History were accepted on Fedora GNOME. Live words require Scribe realtime, or batch previews with Live rewrite enabled. Automatic insertion remains Experimental and disabled by default. | [Capture workflow](../linux/mluva_linux/workflow.py), [delivery tests](../linux/tests/test_delivery.py), [platform boundary](linux-platform-profile.md). |
| Rewriting | Polish, Structure, saved prompts and custom follow-ups transform the latest saved working version. Completed rewrites stay with the original and copy automatically by default. | Experimental. Review model output. Partial responses are display-only; failures/cancellation never become successful replies. Incognito disables rewriting. | [`_request_rewrite` / `_rewrite_finished`](../linux/mluva_linux/app.py), [conversation persistence](../linux/mluva_linux/conversation.py), [lifecycle checks](../linux/tests/conversation_lifecycle.py). |
| Polish existing text | Paste text into a new conversation, then Polish or supply an instruction. Edit and save either the original or a completed reply; Ctrl+S saves, Ctrl+Enter sends. | Implemented, Experimental. Import is explicit; it does not read another application's selected text. Save is local and does not invoke a model. | [`_start_pasted_conversation`](../linux/mluva_linux/app.py), [editable workspace](../linux/mluva_linux/conversation_view.py), [editing fixture](../linux/tests/live_workspace_smoke.py). |
| Live rewrite | Opt in beside Dictate. Task spec, Structured note, Polish and Custom drafts update while speaking; task/note templates expose missing details. Stop reconciles against the complete committed transcript. | Experimental, off by default. Provisional speech is sent to the rewrite provider; only a successful final rewrite may auto-copy. Updates are grouped, and batch STT updates by chunk. This is not an instant or word-for-word guarantee. | [scheduler and prompts](../linux/mluva_linux/live_rewrite.py), [final reconciliation tests](../linux/tests/test_live_rewrite.py), [live fixture](../linux/tests/live_workspace_smoke.py), [behavior contract](providers-and-live-rewrite.md#live-rewrite). |
| Floating / hover controls | The Omarchy widget previews five lines during recording. Completed-note controls offer Polish, Structure, More, Copy, Open and Cancel while rewriting. Hover/focus pauses the four-second default dismissal. | Experimental. It operates on Mluva's captured note. There is no universal toolbar attached to selected text in other apps. Recording passes input through; the GNOME bottom bar is display-only. | [Quickshell implementation](../linux/quickshell/mluva.dictation/RecordingOverlay.qml), [shell contract](omarchy-integration.md), [runtime fixture](../linux/tests/shell_overlay_smoke.py). |
| Expanded text / editing | Open the floating note into the main workspace; scroll/select full text and edit source or results. Wide Live layouts show both panes; narrow layouts stack them. | Implemented, Experimental. No dedicated full-screen editor action is implemented; demonstrate the expanded workspace, not a full-screen feature. | [document editor and layout](../linux/mluva_linux/conversation_view.py), [live wide/narrow checks](../linux/tests/live_workspace_smoke.py). |
| Quiet Live workspace | Equal panes keep their widths through wrapping and final reconciliation. Incoming text eases upward as its final line fills; each scrollbar has a reserved gutter. A centered red pulse and timer mark recording. | Manual scrolling suspends following. Native motion follows system animation settings; shell motion follows the configured smooth-scrolling preference. These changes do not establish microphone or live-compositor acceptance. | [layout checks](../linux/tests/live_layout_smoke.py), [recording control](../linux/mluva_linux/recording_control.py), [shell light](../linux/quickshell/mluva.dictation/RecordingLight.qml). |
| Minimal Markdown | Completed rewrites and Live drafts use restrained headings, emphasis and code styling. Focus reveals editable source; Copy and Save retain exact Markdown. | A small safe Markdown subset, not a browser or full document renderer. Raw recognition remains literal; incomplete live recognition is display-only. | [source parser](../linux/mluva_linux/minimal_markdown.py), [native editor](../linux/mluva_linux/markdown_view.py), [source-preservation tests](../linux/tests/test_minimal_markdown.py). |
| Ctrl+P commands | Search recording, Live, Polish, custom rewrite, Copy, Save, History and Settings. Arrows select, Enter runs, Escape dismisses. | App-local shortcut. Document actions require the same visible conversation and editor when dispatched; busy and provisional states disable unavailable actions. | [command panel](../linux/mluva_linux/command_palette.py), [native keyboard checks](../linux/tests/command_palette_smoke.py). |
| History / search | Search titles, original/working text, instructions and completed rewrites. Show more reaches older records; rename and reopen a conversation. | History has Fedora acceptance. Conversation search and model-generated titles remain Experimental. Local SQLite storage; Incognito saves no new history. Titles may send up to 6,000 source characters to the rewrite provider. | [`ConversationStore.search`](../linux/mluva_linux/conversation.py), [search/restart tests](../linux/tests/test_conversation.py), [title contract](conversation-workspace.md#automatic-conversation-titles). |
| Export | Open History, expand an entry, choose Export Markdown or Export JSON. Export includes raw recognition, saved working source and completed replies. | Implemented, Experimental. Local files in the application's exports directory; no direct Google Docs/Notion publishing or PDF/DOCX export. Save editor changes first. | [`HistoryStore.export`](../linux/mluva_linux/history.py), [export controls](../linux/mluva_linux/history_view.py), [JSON/Markdown lifecycle checks](../linux/tests/conversation_lifecycle.py). |

## Provider matrix

Speech and rewriting are configured independently in **Settings → Providers**. Model discovery is optional; a listed model is not proof that the configured account can run it. See the [selection guide](provider-selection.md) for model aliases and connection settings.

| Task and route | Implemented compatibility | Evidence and deployment limit |
| --- | --- | --- |
| Linux STT · ElevenLabs Scribe | Native realtime plus controlled batch fallback; Meeting diarization still uses ElevenLabs. | Default cloud route. [Native clients](../linux/mluva_linux/realtime.py), [batch client](../linux/mluva_linux/elevenlabs.py). Real provider recordings have [capture provenance](promotion/README.md); they are examples, not a quality benchmark. |
| Linux STT · Voxtype / local Whisper | Runs installed Voxtype in local Whisper file mode. Ordinary dictation recognizes at Stop; Live rewrite enables sequential chunk previews before full-audio reconciliation. | Requires the executable and an installed suitable model. A local JFK fixture run is recorded in the [verification notes](providers-and-live-rewrite.md#verification-boundary); it does not establish quality across languages/models. |
| Linux STT · compatible API | Multipart `POST /audio/transcriptions` with WAV/model and a JSON `text` response. Endpoint, key reference and model are independent of rewriting. | Controlled HTTP tests. Requires an actual transcription deployment: a chat-only endpoint is insufficient. No universal cloud-provider claim. |
| Linux LLM · Codex app-server | Authenticated local client with model discovery, streaming rewrites/titles/cleanup, and Fast only when advertised by that model. | App-server is a client boundary; inference is not necessarily local. Account/model access and credits still apply. [Client](../linux/mluva_linux/codex_client.py), [model selection](../linux/mluva_linux/rewrite_settings.py). |
| Linux LLM · compatible API | Streaming `POST /chat/completions`; optional `GET /models`. Requires a configured text deployment that finishes with `stop`; tool calls and truncated replies fail. | Controlled HTTP tests establish the protocol, not every vendor/account. Mluva neither provisions LiteLLM nor downloads models. |
| macOS source preview | Apple Speech or Google Cloud STT; Gemini Flash cleanup/style/command adapters. | Separate Swift implementation, not parity with Linux provider settings or Live rewrite. No current accepted signed/notarized public binary. [STT factory](../Sources/Services/TranscriptionProviderFactory.swift), [Gemini adapters](../Sources/Services/GeminiFlashRewriteProvider.swift), [macOS guide](macos-development.md). |

The Linux compatible API and Voxtype routes share [provider adapters](../linux/mluva_linux/providers.py) and [controlled transport tests](../linux/tests/test_provider_workspace.py). Local speech alone does not make rewrites or automatic titles local. Hosted compatibility depends on the selected server, deployment, credentials and response protocol.

## Campaign framing

**Direction:** “The most delightful dictation app for Omarchy.” This is the product ambition, not a measured ranking. Pair it with concrete behavior: **“Speak a rough idea. Shape it into useful text. Keep the original.”**

Show existing-text polish, a clearly labeled Live draft, the floating note opening into editable text, searchable history, local export and independent provider settings. Label offline feature captures **“Feature walkthrough · example text”**. The accompanying media README and manifest must disclose the production UI with synthetic text and a local provider fixture, and distinguish it from real-provider footage. Use source/release labels that match each capture. Do not infer speed, accuracy, universal compatibility or live-desktop acceptance from a fixture.

The product name remains Mluva; the conflicting dictated spellings do not introduce a rename. The selected mark from PR #17 is retained. The [brand contract](brand-and-compatibility.md) records the integration surfaces, generation provenance and preserved upgrade identities.
