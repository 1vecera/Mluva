# Intro rebuild — improvement log

Each row is a completed change followed by inspection. The prior film’s claimed iterations are not counted. Evidence lives in `launch-video/out/` and private `tmp/overnight/`; final verification has its own report. Still inspections establish appearance, not motion or listening quality.

| Pass | Defect and change | Evidence reviewed | Decision / remaining issue |
| --- | --- | --- | --- |
| 01 | Prior React replicas invented workspace/provider geometry. Replaced them with production GTK captures, registered native regions and a seven-beat composition. Removed eleven separate scene implementations and old camera/water systems. | Prior `tmp/overnight/baseline.png`; `out/pass-01-native.png`, 14 s. | Keep native geometry. App is soft at this magnification and reflections show through its translucent body. |
| 02 | Native alpha was composited onto the film backdrop; 1× capture text blurred on zoom. Backed the native surface with capture-desktop black and recaptured at 2× without changing logical layout. | `out/pass-02-backed.png`, same 14 s; compared text, window edges and blank body to pass 01. | Keep. Text is materially sharper and reflections no longer alter app colours. Still-state changes and live fixture semantics need correction. |
| 03 | Still jumps hid progressive input; Grilling fixture omitted its required Architecture heading. Recaptured at 2× with incremental dictation, actual Polish/Structure button callbacks against a synthetic streaming provider, and the correct question/body split. Shortened example content and restored the native source scroll origin. | `out/pass-03-native-progress.mp4` (21–29.2 s), `out/pass-03-grilling.png` (25.5 s), native-v6 polished and Grilling captures. | Keep corrected structure and short copy. The question is visible but the later camera push consumes reading time. Native screen-video samples exposed an Xvfb hover tooltip; recapture with the private pointer outside the app. |
| 04 | The old procedural haze and replacement still provided no convincing moving material. Built and rendered an original Blender wave surface with long red/silver reflections; integrated its six-second source loop at half speed. | `out/pass-04-background.mp4` (0–13 s), `out/pass-04-brand.png` (3 s), `tmp/overnight/background/loop-seam.jpg` and `loop-metrics.json`. | Keep the continuous material. Wrap difference 0.468/255 is below mean adjacent 0.621/255; no wrap outlier in the ordered source-frame sheet. Reflections compete with the opening subtitle, so lower contrast behind brand text in a later hierarchy pass. |
| 05 | Speech was paced by eleven cut segments in the old film; initial new copy overstated saved-history/provider scope. Rewrote the script, reduced seven tone directions to three, retained a continuous take, added the Experimental qualifier, and derived scene/action times from actual Scribe words. Selected the 42.916 s take with explicit brand phonemes after an earlier candidate transcribed the name as “Mloover.” | `out/pass-05-timing.mp4` (10–29.2 s), `out/pass-05-sheet.jpg`, v5 word transcript versus canonical script (exact normalized agreement). | Keep phrasing, continuous audio and transcript timing. This establishes text/timing, not auditory approval. Scene-boundary fades still make the app disappear, and small-player text needs closer framing. |
| 06 | Repeated scene fades briefly removed the app; camera movement competed with reading, and the question was too small. Kept the app opaque between adjacent scenes, increased detail scale to 1.664×, and completed the Live camera move before the question appears. | `out/pass-06-framing.mp4` (10–29.2 s), `out/pass-06-live.png` (26.5 s). | Keep the stable larger question and continuous window. Full-resolution inspection exposed hairline seams between the three raster bands at fractional scale; replace the unnecessary band assembly. The capture clock also needs to advance instead of freezing. |
| 07 | Fractional scaling revealed band-edge seams and the recording clock froze on a still value. Removed the three-band abstraction, preserved each complete native raster, and composited an opaque, native-rendered clock patch with matching black backing. | `out/pass-07-native-layers.mp4` (22–29.2 s), `out/pass-07-live.png` (26.5 s), compared with the pass 06 still. | Keep: seam lines disappear and elapsed seconds advance using captured glyphs. A separate screen-video registration check exposed a roughly 1% capture-size squeeze; switch the next capture to the paintable's intrinsic size before final fidelity certification. |
| 08 | The model shot showed only speech settings and claimed a theme change without evidence. Captured the actual Providers settings page and Nord/Tokyo Night/Rosé Pine states; added the native history-opening transition. Corrected capture dimensions to the paintable’s intrinsic 1040×640 logical size. | `out/pass-08-providers-themes.mp4` (29.13–40 s), `out/pass-08-providers.png`, `out/pass-08-theme.png`; `native-v11/registration.json`. | Keep: both provider names and actual palettes are visible. Screen/capture affine registration is now effectively identity (0.9999998× / 1.0000067×, translation under .002 px in the measured region). Provider framing clips the top controls; fix its anchor. Theme cuts are brief but only colour, not new text, is the reading target. |
| 09 | The opening waited too long to show the app; “editable” had no visible action, and instant rewrite states lacked a click cue. Brought the native window forward to 3.84 s, showed genuine text selection, added registered cursor cues at Polish/Structure, aligned F9 with the spoken key, and used camera curves with zero endpoint velocity. Corrected the provider anchor and frame-rate-dependent history offset. | `out/pass-09-actions.mp4` (2.4–17.83 s), `out/pass-09-editable.png` (12.03 s); comparison to passes 05/08. | Keep the clearer action/result relationships and earlier product view. The opening cut still needs an overlapping handoff so a zero-opacity entrance cannot leave a blank beat; finish background hierarchy and the mixed soundtrack next. |
| 10 | Bright reflections competed with brand copy and the opening handoff could briefly empty the frame. Added a central contrast mask, reduced background intensity during reading, overlapped the opening handoff, and kept the closing card immediately visible. Reduced the voice gain before mixing (its measured true peak was −0.01 dBTP), restrained music/click levels, and rendered the complete story. | `out/pass-10-full-preview.mp4`, `out/pass-10-storyboard.jpg`, source voice/music loudness measurements. | Keep hierarchy and unclipped mix headroom. Full-story review exposed a genuine defect: Structure was saved below the visible scroll position. Independent fidelity checks passed all 40 static states exactly but caught half-pixel resampling in the tiny timer patch. Correct both before the final master; fix the capped outro music rise as well. |
| 11 | Structure was below the viewport and tiny clock crops resampled on half-CSS-pixel boundaries. Restored native focus/scroll before capture, aligned patch bounds to capture density, corrected caption punctuation and the outro music rise; rendered and mastered the full 60 fps film. | `out/pass-11-full-master.mp4`, `out/pass-11-review/`, `out/pass-11-fidelity.json`: 43 exact comparisons; 2,804 frames decoded with valid cadence; mix transcript matched; −16.14 LUFS, −1.81 dBTP. | Keep the corrected structured note, clock and mix. Complete-story inspection caught a separate live-draft viewport problem: Questions were visible but Architecture was scrolled offscreen. Frame-difference diagnostics also identified the bright-theme cut as abrupt. Correct these rather than treating pixel equality as sufficient acceptance. |
| 12 | Grilling's draft body was outside the native viewport; the bright-theme and closing cuts produced large luminance steps. Captured actual reader navigation to the draft's beginning, asserted that the expected body exists, added a 0.32 s theme dissolve and 0.4 s closing handoff, and eased backdrop intensity with the brand handoffs. | Final `out/mluva-intro-master.mp4`, `out/final-review/`, committed `review/storyboard.jpg`, `review/comparison.png`, `review/grilling-720.png`, `review/metrics.json` and `review/fidelity.json`. Inspected action/camera/cut windows and 720p focal frames; the question and Architecture body are now both visible. | Keep. All 43 native comparisons remain exact. The largest grayscale frame step falls from 95.47 to 12.80/255 (diagnostic, not a perceptual score). All 2,804 frames/audio decode, cadence is valid, and the encoded audio stream is identical to the transcribed pass 11 mix. No remaining concrete defect was identified in the reviewed evidence; natural acting and real-time viewing remain perceptual review items, not machine-certified claims. |
